# Skip optional dependencies. drizzle-orm/drizzle-kit list several optional
# database drivers (better-sqlite3, pg, mysql2, ...) as optional peer deps for
# features this project doesn't use; some require a native build toolchain
# to install. Our own code only uses Node's built-in node:sqlite module.
omit=optional
